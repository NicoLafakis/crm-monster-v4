import React from 'react'

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui, Arial, sans-serif' }}>
      <h1>CRM Monster — React (scaffold)</h1>
      <p>This is a minimal Vite + React scaffold. Use <code>npm run dev</code> inside <code>web/</code> to start.</p>
    </div>
  )
}
